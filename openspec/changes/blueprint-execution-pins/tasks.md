## 1. Property Promote

- [x] 1.1 Add shared declared property metadata, safe stable promoted pin IDs, instance persistence/validation, and immutable execution overlays; verify typed/falsy/default/nested and rejection cases in both engines.
- [x] 1.2 Add adjacent localized property controls and effective pins through load/paste/subflows/history; verify restore-with-wires undo and persistence with client tests.
- [x] 1.3 Build and inspect Promote, fallback, wiring, restore/undo, and reload in a real isolated DSH plugin; retain screenshots and runtime evidence before proceeding.

## 2. Multiple flow inputs

- [x] 2.1 Separate flow connection cardinality from data aggregation and implement independent Blueprint activations behind an explicit semantics flag; verify alternate/simultaneous triggers, scalar payloads and explicit Join.
- [x] 2.2 Add execution semantics selection and new-workflow defaults with truthful descriptions; verify connection feedback and compatibility serialization.
- [x] 2.3 Build and inspect multiple incoming flow wires plus their separate execution evidence in the real DSH plugin before proceeding.

## 3. Blueprint node model

- [x] 3.1 Classify built-ins and providers, complete effectful flow pins preserving first-port IDs, and add usable pure values; verify HTTP completion/failure and shared catalog agreement.
- [x] 3.2 Implement invocation-local pure dependency evaluation, data/flow separation and checkpoint-safe call provenance; verify pure cycles, inactive effects, loop state, pause/resume and cancellation regressions.
- [x] 3.3 Refine node presentation and built-in examples for Blueprint flow chains; verify real DSH HTTP-to-successor with promoted inputs, pure values and representative viewport/keyboard checks.

## 4. Assurance and integration

- [x] 4.1 Complete independent spec/code/security review, resolve findings, and run fresh typecheck, full tests, plugin/preview builds and strict OpenSpec validation.
- [x] 4.2 Document semantics, compatibility, tested evidence, limitations and rollback; integrate only owned changes preserving previous chunk work, rebuild the linked plugin and verify clean debug registrations/processes.
